the machine only reads files named "RUN.G"

please make sure the file you want to draw is named exactly that way, no "." extensions after that.

drag your desired ".gcode" into the root of the sd card, and rename it to "RUN.G".

then plug the sd card into the zedboard (after programming the PS and PL obviously) and the machine after pressing start will plot your png.